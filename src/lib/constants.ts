/** Shared constants used across the frontend */

/** Default proxy server port */
export const DEFAULT_PROXY_PORT = 9339;

/** Default task timeout in milliseconds */
export const DEFAULT_TIMEOUT_MS = 30000;

/** Default suite step timeout in milliseconds */
export const DEFAULT_STEP_TIMEOUT_MS = 60000;

/** Streaming task safety timeout in milliseconds (5 minutes) */
export const STREAM_TIMEOUT_MS = 5 * 60 * 1000;
