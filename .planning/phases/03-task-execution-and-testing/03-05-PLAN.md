---
phase: 03-task-execution-and-testing
plan: 05
type: execute
wave: 3
depends_on: ["03-03", "03-04"]
files_modified:
  - src/components/settings/SettingsModal.tsx
  - src/components/test/TestPanel.tsx
  - src/components/layout/Sidebar.tsx
  - src/App.tsx
autonomous: false
requirements:
  - SETT-01
  - SETT-02
  - WORK-01
  - UIUX-03
  - UIUX-04

must_haves:
  truths:
    - "Settings modal shows global settings: timeout, proxy, theme, telemetry"
    - "Per-card settings accessible: default auth headers, base URL override"
    - "Workspace selector in sidebar footer lets user switch workspaces"
    - "Switching workspace filters the agent list"
    - "User can create and delete workspaces"
    - "Keyboard shortcuts work: Cmd+N adds agent, Cmd+Enter runs test, Cmd+Shift+C copies curl"
    - "Empty states guide new users with example AIGC agent URL"
    - "Empty state shows when no agents, no skills selected, no test results"
  artifacts:
    - path: "src/components/settings/SettingsModal.tsx"
      provides: "Global and per-card settings UI"
      min_lines: 60
  key_links:
    - from: "src/App.tsx"
      to: "src/hooks/useKeyboardShortcuts.ts"
      via: "Wiring keyboard shortcuts to app-level actions"
      pattern: "useKeyboardShortcuts"
    - from: "src/components/layout/Sidebar.tsx"
      to: "src/stores/workspaceStore.ts"
      via: "Workspace selector dropdown"
      pattern: "useWorkspaceStore"
    - from: "src/components/settings/SettingsModal.tsx"
      to: "src/bindings.ts"
      via: "commands.getSettings, commands.saveSetting"
      pattern: "commands\\.getSettings|commands\\.saveSetting"
---

<objective>
Wire the final Phase 3 features: settings modal (global + per-card), workspace selector in sidebar, keyboard shortcuts at app level, and empty states throughout the app. Then visual verification checkpoint.

Purpose: These are the polish features completing all v1 requirements. Settings, workspaces, shortcuts, and empty states are the final touches before the app is feature-complete.

Output: Settings modal, workspace selector, keyboard shortcuts wired, empty states, visual checkpoint
</objective>

<execution_context>
@/Users/orange/.claude/get-shit-done/workflows/execute-plan.md
@/Users/orange/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-task-execution-and-testing/03-CONTEXT.md

@src/App.tsx
@src/components/layout/Sidebar.tsx
@src/bindings.ts

<interfaces>
<!-- From 03-02 -->
```typescript
// workspaceStore
export const useWorkspaceStore = create<WorkspaceState>()(persist((set, get) => ({
  workspaces: [], activeWorkspaceId: "default",
  loadWorkspaces, createWorkspace, deleteWorkspace, setActiveWorkspace,
}), { name: "a2a-workspace-state" }));

// useKeyboardShortcuts
export function useKeyboardShortcuts(handlers: {
  onAddAgent?: () => void; onRunTest?: () => void; onCopyCurl?: () => void;
}): void;
```

<!-- From 03-02 bindings -->
```typescript
commands.getSettings(): Promise<Record<string, unknown>>
commands.saveSetting(key: string, value: string): Promise<void>
```

<!-- CONTEXT.md locked empty states -->
No agents: "Get started by adding your first A2A agent" + https://aigc-service.echonlab.com
No skills: "Select an agent to browse its skills"
No test results: "Select a skill and run a test to see results here"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SettingsModal, add workspace selector to Sidebar, wire empty states and keyboard shortcuts</name>
  <files>
    src/components/settings/SettingsModal.tsx,
    src/components/layout/Sidebar.tsx,
    src/components/test/TestPanel.tsx,
    src/App.tsx
  </files>
  <read_first>
    src/App.tsx,
    src/components/layout/Sidebar.tsx,
    src/components/test/TestPanel.tsx,
    src/stores/uiStore.ts,
    src/bindings.ts,
    .planning/phases/03-task-execution-and-testing/03-CONTEXT.md
  </read_first>
  <action>
    1. **SettingsModal.tsx**: Modal overlay component. Props: `{ isOpen: boolean; onClose: () => void; cardId?: string }`.
       - **Global settings** (when no cardId):
         - Default timeout (ms): number input, default 30000. Key: "timeout_seconds", store as JSON number.
         - Proxy URL: text input, optional. Key: "proxy_url", store as JSON string or null.
         - Theme: dropdown system/light/dark. Key: "theme". Also updates uiStore.setThemeOverride.
         - Telemetry opt-in: checkbox, default off. Key: "telemetry_enabled".
         - On mount, load current values via `commands.getSettings()`.
         - On change, save immediately via `commands.saveSetting(key, JSON.stringify(value))`.
       - **Per-card settings** (when cardId provided):
         - Default auth header: text input. Key: `card:${cardId}:auth_header`.
         - Base URL override: text input. Key: `card:${cardId}:base_url_override`.
         - Same save pattern with card-prefixed keys.
       - Styled as a centered modal with backdrop, max-width 480px, padding 24px, rounded corners.
       - Close on backdrop click or Escape key.

    2. **Sidebar.tsx**: Add workspace selector at bottom of sidebar:
       - Import `useWorkspaceStore`
       - Call `loadWorkspaces()` on mount
       - Render dropdown at sidebar footer showing workspace name
       - Switching workspace calls `setActiveWorkspace(id)` and reloads agents via `agentStore.loadAgents(newWorkspaceId)`
       - "+" button next to dropdown: prompt for name, call `createWorkspace(name)`
       - Settings gear icon button: opens SettingsModal (add local state `settingsOpen`)
       - Empty state for no agents: "Get started by adding your first A2A agent" with clickable example URL `https://aigc-service.echonlab.com` that triggers addAgent flow

    3. **TestPanel.tsx**: Wire remaining features:
       - Import and render ResponseViewer in the right column (replacing placeholder from 03-03)
       - Import and render HistoryList below response (or in a collapsible section)
       - Import and render SavedTestsList in the input column below the input form
       - Empty state when no skill selected: "Select a skill to begin testing"
       - Empty state when no result: "Select a skill and run a test to see results here" (in right column)
       - Wire "Save test" button that creates a saved test from current request/response
       - Wire SavedTestsList onRerun: populate inputText and trigger send

    4. **App.tsx**: Wire keyboard shortcuts at the top level:
       - Import `useKeyboardShortcuts`
       - Cmd+N: open AddAgentDialog (or trigger the add agent flow — check how it's currently opened)
       - Cmd+Enter: trigger Run test (call a ref or store action)
       - Cmd+Shift+C: trigger copy curl (call generateCurlCommand + clipboard)
       - Add shortcut hints to button tooltips throughout the app (title attributes)
  </action>
  <verify>
    <automated>cd /Users/orange/Documents/code/startup/a2a-workbench && npx vitest run --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'getSettings\|get_settings' src/components/settings/SettingsModal.tsx
    - grep -q 'saveSetting\|save_setting' src/components/settings/SettingsModal.tsx
    - grep -q 'timeout\|proxy\|theme\|telemetry' src/components/settings/SettingsModal.tsx
    - grep -rq 'card:.*:auth_header\|cardId.*auth' src/components/settings/SettingsModal.tsx
    - grep -q 'useWorkspaceStore' src/components/layout/Sidebar.tsx
    - grep -q 'aigc-service.echonlab.com\|Get started' src/components/layout/Sidebar.tsx
    - grep -q 'ResponseViewer' src/components/test/TestPanel.tsx
    - grep -q 'HistoryList' src/components/test/TestPanel.tsx
    - grep -q 'SavedTestsList' src/components/test/TestPanel.tsx
    - grep -q 'useKeyboardShortcuts' src/App.tsx
    - npx vitest run exits with code 0
  </acceptance_criteria>
  <done>Settings modal with global and per-card settings, workspace selector in sidebar, keyboard shortcuts wired, empty states with AIGC example URL, all components integrated into TestPanel. All tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete Phase 3</name>
  <files>none</files>
  <action>Human visually verifies the complete Phase 3 implementation by running through the full test workflow in the running app.</action>
  <verify>
    <automated>cd /Users/orange/Documents/code/startup/a2a-workbench && npx vitest run 2>&1 | tail -3</automated>
  </verify>
  <what-built>Complete test execution workflow: adaptive input form with tabs (message/context data/headers), Monaco JSON editor for data mode, auth selection, Run button with status indicators, response viewer with rendered markdown and raw JSON tree, history list with search and clear, saved tests with re-run, settings modal, workspace selector, keyboard shortcuts, and empty states.</what-built>
  <how-to-verify>
    1. Run `npm run tauri dev` to start the app
    2. Verify empty state: "Get started by adding your first A2A agent" with AIGC example URL
    3. Add the AIGC agent: https://aigc-service.echonlab.com
    4. Select a skill — verify skill metadata appears (name, id, description, modes)
    5. Type a test message and click Run — verify status indicator changes (green -> amber -> green)
    6. Verify response appears in rendered tab with formatted content
    7. Switch to "raw json" tab — verify collapsible JSON tree with correct colors
    8. Check "context data" tab — verify Monaco JSON editor loads (may take a moment)
    9. Check "headers" tab — verify key-value editor
    10. Press Cmd+Shift+C — verify curl command copied to clipboard
    11. Check history list shows the execution you just ran
    12. Save a test case — verify it appears in saved tests list
    13. Click re-run on saved test — verify it fills input and sends
    14. Open settings — verify global settings (timeout, proxy, theme, telemetry)
    15. Check workspace selector in sidebar footer
    16. Try keyboard shortcuts: Cmd+N (add agent), Cmd+Enter (run test)
    17. Test dark mode — verify JSON tree colors change appropriately
  </how-to-verify>
  <done>User has visually confirmed all Phase 3 features work correctly end-to-end</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Full visual walkthrough of the complete test execution workflow.
</verification>

<success_criteria>
- Settings modal works for global and per-card settings (SETT-01, SETT-02)
- Workspace selector visible and functional (WORK-01)
- Keyboard shortcuts work (UIUX-03)
- Empty states guide new users (UIUX-04)
- Full test workflow works end-to-end against AIGC agent
</success_criteria>

<output>
After completion, create `.planning/phases/03-task-execution-and-testing/03-05-SUMMARY.md`
</output>
